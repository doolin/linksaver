# Next

Thread index — one pointer line per open thread, no narrative.
The narrative lives in `threads/<thread>.md`. On resume, read
only the thread file for the work you were asked to do; never
bulk-read `threads/`.

<!-- No open threads. -->
