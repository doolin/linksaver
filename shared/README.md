# Shared Logic

This directory will hold browser-agnostic logic shared across all browser extensions (e.g., date-stamped folder naming, bookmark organization logic).

Currently empty — the Firefox extension is self-contained. When a second browser is added, common logic will be extracted here.

See [`docs/architecture.md`](../docs/architecture.md) for the shared code strategy.
